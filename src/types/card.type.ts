export type CARD = {
  name: string;
  cartNumber: string;
  releaseDate: string;
  bankCode: string;
};

export type BANK_INFOR = {
  id: number;
  createDate: null;
  status: boolean;
  bankCode: string;
  url_img: string;
};
