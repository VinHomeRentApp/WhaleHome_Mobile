export enum AUTH_API_ERROR {
  INVALID_PASSWORD = 'auth/invalid-password',
  INVALID_CREDENTIAL = 'auth/invalid-credential',
  EMAIL_ALREADY_IN_USE = 'auth/email-already-in-use',
  WEAK_PASSWORD = 'auth/weak-password'
}
