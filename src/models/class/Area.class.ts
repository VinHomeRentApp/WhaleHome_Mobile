class Area {
  id: number;
  createDate: string;
  name: string;

  constructor(id: number, createDate: string, name: string) {
    this.id = id;
    this.createDate = createDate;
    this.name = name;
  }
}

export default Area;
